package hex.jglobalkey.handler;

import hex.jglobalkey.JGlobalKeyManager;

public class PressHandler implements GlobalKeyHandler{
	
	@Override
	public void handle(int[]keyIds,JGlobalKeyManager keyManager){
		
	}
}
